Admin section
	- Allow user to change default quota and per user quota
	
Backup/restore
	- User quotas
	- Allow for deletion of multiple files at once
	- Prevent duplicate file metadata (files themselves can not be duplicated as they are simply overwritten)
	- Allow user to create directories
	- Display files in tree
	
User interface
	- Add help section
	- Test on other browsers