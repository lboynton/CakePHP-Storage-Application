Admin section
	- Allow user to change default quota and per user quota
	
Backup/restore
	- User quotas
	- Allow for deletion of multiple files at once
	- Prevent duplicate files
	- Allow user to create directories
	- Stop zero length filenames when renaming
	- Move files
	
User interface
	- Add help section
	- Test on other browsers